import { z } from 'zod';
import { ANALYTICS_INTERVALS, ANALYTICS_PRESETS, ANALYTICS_REPORTS } from '../constants/analytics.js';
const objectId = z.string().regex(/^[a-f\d]{24}$/i, 'Invalid identifier');
export const analyticsQuery = z.object({ from: z.iso.datetime().optional(), to: z.iso.datetime().optional(), preset: z.enum(ANALYTICS_PRESETS).optional(), timezone: z.literal('UTC').default('UTC'), interval: z.enum(ANALYTICS_INTERVALS).optional(), companyId: objectId.optional(), jobId: objectId.optional(), role: z.enum(['candidate', 'recruiter', 'admin']).optional(), status: z.string().trim().max(50).optional(), category: z.string().trim().max(80).optional(), industry: z.string().trim().max(150).optional(), workMode: z.enum(['onsite', 'remote', 'hybrid']).optional(), employmentType: z.string().trim().max(50).optional(), eventType: z.string().trim().max(100).optional() }).strict().superRefine((value, context) => { if (Boolean(value.from) !== Boolean(value.to)) context.addIssue({ code: 'custom', message: 'from and to must be provided together', path: ['from'] }); if (value.preset && value.from) context.addIssue({ code: 'custom', message: 'preset cannot be combined with custom dates', path: ['preset'] }); });
export const exportQuery = analyticsQuery.safeExtend({ report: z.enum(ANALYTICS_REPORTS), format: z.enum(['json', 'csv']).default('json') });
