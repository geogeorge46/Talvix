import{env}from'../config/env.js';import{Document}from'../models/Document.js';import{AppError}from'../shared/errors/AppError.js';export const assertQuota=async(owner,incoming,replacing=0)=>{const rows=await Document.aggregate([{$match:{owner,status:{$in:['active','archived','quarantined']}}},{$group:{_id:null,bytes:{$sum:'$sizeBytes'}}}]);const used=rows[0]?.bytes??0;const maximum=env.FILE_MAX_USER_STORAGE_MB*1024*1024;if(used-replacing+incoming>maximum)throw new AppError('Document storage quota exceeded',413);return{usedBytes:used,maximumBytes:maximum};};
