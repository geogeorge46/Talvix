import{InterviewFeedback}from'../models/InterviewFeedback.js';import{InterviewProcess}from'../models/InterviewProcess.js';import{InterviewRound}from'../models/InterviewRound.js';import{InterviewSchedule}from'../models/InterviewSchedule.js';import{AppError}from'../shared/errors/AppError.js';export const processes=()=>InterviewProcess.find().sort({createdAt:-1});export const process=async id=>{const x=await InterviewProcess.findById(id);if(!x)throw new AppError('Interview process not found',404);return x;};export const schedule=async id=>{const x=await InterviewSchedule.findById(id).select('+interviewerInstructions +meetingPassword');if(!x)throw new AppError('Interview schedule not found',404);return x;};const update=async(Model,id,status,u,reason)=>{const x=await Model.findById(id);if(!x)throw new AppError('Interview record not found',404);x.audit.push({action:'admin-status-correction',from:x.status,to:status,actor:u,reason,at:new Date()});x.status=status;await x.save();return x;};export const processStatus=(id,s,u,r)=>update(InterviewProcess,id,s,u,r);export const roundStatus=(id,s,u,r)=>update(InterviewRound,id,s,u,r);export const cancelSchedule=(id,u,r)=>update(InterviewSchedule,id,'cancelled',u,r);export const reopenFeedback=async(id,u,reason)=>{const f=await InterviewFeedback.findById(id);if(!f)throw new AppError('Interview feedback not found',404);if(!f.submitted)throw new AppError('Feedback is already editable',409);f.audit.push({action:'admin-reopened',actor:u,reason,at:new Date(),previousVersion:f.version});f.submitted=false;f.version+=1;await f.save();return f;};
