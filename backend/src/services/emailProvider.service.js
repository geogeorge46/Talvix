import{env}from'../config/env.js';import{logger}from'../shared/utils/logger.js';import{normalizeEmailError}from'../utils/emailError.js';const disabled={sendEmail:async()=>({provider:'disabled',status:'suppressed'})};const consoleAdapter={sendEmail:async({to,subject,idempotencyKey})=>{logger.info(`Email metadata: recipientDomain=${to.split('@')[1]??'invalid'} subjectLength=${subject.length} idempotency=${idempotencyKey}`);return{provider:'console',providerMessageId:`console-${idempotencyKey}`,status:'sent'};}};const resend={sendEmail:async input=>{const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),10000);try{const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.RESEND_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':input.idempotencyKey},body:JSON.stringify({from:`${env.EMAIL_FROM_NAME} <${env.EMAIL_FROM_ADDRESS}>`,to:[input.to],subject:input.subject,html:input.html,text:input.text,reply_to:input.replyTo||undefined,tags:input.tags}),signal:controller.signal});if(!response.ok){const e=new Error('Provider request failed');e.status=response.status;throw e;}const data=await response.json();return{provider:'resend',providerMessageId:data.id,status:'sent'};}catch(error){throw Object.assign(new Error('Email provider error'),normalizeEmailError(error));}finally{clearTimeout(timeout);}}};export const getEmailProvider=()=>!env.EMAIL_ENABLED?disabled:env.EMAIL_PROVIDER==='resend'?resend:env.EMAIL_PROVIDER==='console'?consoleAdapter:disabled;export const sendEmail=input=>getEmailProvider().sendEmail(input);
