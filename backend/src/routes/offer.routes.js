import{Router}from'express';import{USER_ROLES}from'../constants/roles.js';import*as ad from'../controllers/adminOffer.controller.js';import*as ap from'../controllers/offerApproval.controller.js';import*as c from'../controllers/candidateOffer.controller.js';import*as o from'../controllers/offer.controller.js';import*as an from'../controllers/offerAnalytics.controller.js';import*as t from'../controllers/offerTemplate.controller.js';import{authenticate}from'../middleware/auth.js';import{authorizePermissions}from'../middleware/authorizePermissions.js';import{authorizeRoles}from'../middleware/authorizeRoles.js';import{requireCompanyAccess}from'../middleware/companyAccess.js';import*as v from'../validators/offer.validator.js';import{validateBody,validateParams,validateQuery}from'../validators/validate.js';export const offerRouter=Router();offerRouter.use(authenticate);const rec=p=>[authorizeRoles(USER_ROLES.RECRUITER),authorizePermissions(p),requireCompanyAccess];const cand=authorizeRoles(USER_ROLES.CANDIDATE);const admin=authorizeRoles(USER_ROLES.ADMIN);
offerRouter.get('/admin',admin,ad.list);offerRouter.get('/admin/:offerId',admin,validateParams(v.id),ad.get);offerRouter.patch('/admin/:offerId/status',admin,validateParams(v.id),validateBody(v.adminStatus),ad.status);offerRouter.patch('/admin/:offerId/expire',admin,validateParams(v.id),validateBody(v.reason),ad.expire);offerRouter.patch('/admin/:offerId/reopen',admin,validateParams(v.id),validateBody(v.reason),ad.reopen);offerRouter.patch('/admin/:offerId/archive',admin,validateParams(v.id),validateBody(v.reason),ad.archive);
offerRouter.get('/analytics',...rec('offers.view'),an.analytics);offerRouter.get('/approvals',...rec('offers.approve'),ap.list);offerRouter.get('/approvals/:offerId',...rec('offers.approve'),validateParams(v.id),ap.get);offerRouter.patch('/approvals/:offerId/approve',...rec('offers.approve'),validateParams(v.id),validateBody(v.comments),ap.approve);offerRouter.patch('/approvals/:offerId/reject',...rec('offers.approve'),validateParams(v.id),validateBody(v.reason),ap.reject);
offerRouter.post('/templates',...rec('offers.manage'),validateBody(v.templateBody),t.create);offerRouter.get('/templates',...rec('offers.view'),validateQuery(v.query),t.list);offerRouter.get('/templates/:templateId',...rec('offers.view'),validateParams(v.templateId),t.get);offerRouter.patch('/templates/:templateId',...rec('offers.manage'),validateParams(v.templateId),validateBody(v.templateUpdate),t.update);offerRouter.delete('/templates/:templateId',...rec('offers.manage'),validateParams(v.templateId),t.remove);offerRouter.post('/templates/:templateId/clone',...rec('offers.manage'),validateParams(v.templateId),t.clone);
offerRouter.get('/me',cand,validateQuery(v.query),c.list);offerRouter.get('/me/:offerId/timeline',cand,validateParams(v.id),c.timeline);offerRouter.get('/me/:offerId',cand,validateParams(v.id),c.get);offerRouter.get('/me/:offerId/letter/download',cand,validateParams(v.id),c.downloadLetter);offerRouter.patch('/me/:offerId/view',cand,validateParams(v.id),c.view);offerRouter.patch('/me/:offerId/accept',cand,validateParams(v.id),validateBody(v.acceptance),c.accept);offerRouter.patch('/me/:offerId/decline',cand,validateParams(v.id),validateBody(v.decline),c.decline);offerRouter.patch('/me/:offerId/negotiate',cand,validateParams(v.id),validateBody(v.negotiation),c.negotiate);
offerRouter.get('/manage',...rec('offers.view'),validateQuery(v.query),o.list);offerRouter.get('/manage/:offerId/history',...rec('offers.view'),validateParams(v.id),o.history);offerRouter.get('/manage/:offerId/ai-analysis',...rec('offers.view'),validateParams(v.id),o.aiAnalysis);offerRouter.get('/manage/:offerId/letter/preview',...rec('offers.view'),validateParams(v.id),o.previewLetter);offerRouter.get('/manage/:offerId',...rec('offers.view'),validateParams(v.id),o.get);offerRouter.patch('/manage/:offerId',...rec('offers.manage'),validateParams(v.id),validateBody(v.offerUpdate),o.update);offerRouter.patch('/manage/:offerId/request-approval',...rec('offers.manage'),validateParams(v.id),o.requestApproval);offerRouter.patch('/manage/:offerId/send',...rec('offers.send'),validateParams(v.id),ap.send);offerRouter.patch('/manage/:offerId/withdraw',...rec('offers.manage'),validateParams(v.id),validateBody(v.reason),o.withdraw);offerRouter.patch('/manage/:offerId/cancel',...rec('offers.manage'),validateParams(v.id),validateBody(v.reason),o.cancel);offerRouter.patch('/manage/:offerId/archive',...rec('offers.manage'),validateParams(v.id),o.archive);offerRouter.patch('/manage/:offerId/negotiation/resolve',...rec('offers.manage'),validateParams(v.id),validateBody(v.resolution),o.resolve);offerRouter.post('/manage/:offerId/revise',...rec('offers.manage'),validateParams(v.id),validateBody(v.revisionBody),o.revise);offerRouter.patch('/manage/:offerId/confirm-hire',...rec('offers.manage'),validateParams(v.id),o.hire);offerRouter.patch('/manage/:offerId/onboarding/complete',...rec('offers.manage'),validateParams(v.id),o.completeOnboarding);offerRouter.post('/',...rec('offers.manage'),validateBody(v.offerBody),o.create);
