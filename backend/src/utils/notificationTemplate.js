import{AppError}from'../shared/errors/AppError.js';export const escapeHtml=value=>String(value).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const pattern=/{{\s*([a-zA-Z][a-zA-Z0-9]*)\s*}}/g;export const validateTemplate=(body,variables)=>{if(/<script|javascript:|\son\w+\s*=/i.test(body))throw new AppError('Unsafe template HTML is not allowed',400);const used=[...body.matchAll(pattern)].map(x=>x[1]);if(used.some(v=>!variables.includes(v))||variables.some(v=>['__proto__','constructor','prototype'].includes(v)))throw new AppError('Template contains an undeclared or unsafe variable',400);return used;};export const renderTemplate=(body,declared,values)=>{const used=validateTemplate(body,declared);for(const key of used)if(values[key]===undefined||values[key]===null)throw new AppError(`Missing template variable: ${key}`,400);return body.replace(pattern,(_m,key)=>escapeHtml(values[key]));};export const htmlToText=html=>html.replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim();
