const annual={hourly:2080,monthly:12,yearly:1,'one-time':1};export const calculateEstimatedTotal=c=>{const p=c.period;const normalize=i=>i.period===p?i.amount:i.amount*(annual[i.period]/annual[p]);return[c.base,c.variable,c.bonus,c.joiningBonus].reduce((s,v)=>s+(v??0),0)+(c.allowances??[]).reduce((s,i)=>s+normalize(i),0);};export const prepareCompensation=c=>({...c,currency:(c.currency??'INR').toUpperCase(),estimatedTotal:calculateEstimatedTotal(c)});
