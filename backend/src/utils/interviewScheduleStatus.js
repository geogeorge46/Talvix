import { AppError } from '../shared/errors/AppError.js';const transitions={proposed:['confirmed','cancelled'],'confirmed':['reschedule-requested','rescheduled','completed','cancelled','no-show'],'reschedule-requested':['rescheduled','cancelled'],rescheduled:['confirmed','cancelled'],completed:[],cancelled:[],'no-show':[]};export const assertScheduleTransition=(from,to)=>{if(!transitions[from]?.includes(to))throw new AppError(`Interview schedule cannot transition from ${from} to ${to}`,409);};
