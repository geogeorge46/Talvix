import { AppError } from '../shared/errors/AppError.js';const transitions={pending:['scheduling','skipped'],scheduling:['scheduled','cancelled'],scheduled:['in-progress','reschedule-requested','cancelled','no-show'],'reschedule-requested':['scheduled','cancelled'],'in-progress':['awaiting-feedback','completed'],'awaiting-feedback':['completed','cancelled'],completed:[],cancelled:[],skipped:[],'no-show':[]};export const assertRoundTransition=(from,to)=>{if(!transitions[from]?.includes(to))throw new AppError(`Interview round cannot transition from ${from} to ${to}`,409);};
