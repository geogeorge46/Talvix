# Frontend

This directory is reserved for the Talvix React application. It is kept
separate from the backend so the client can be developed, tested, and deployed
independently to Vercel.

Frontend initialization is intentionally deferred to its scheduled milestone.

